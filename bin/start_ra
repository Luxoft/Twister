#!/bin/bash

PYTHON=/usr/bin/python
RA_PORT=8001

if [ -n "$TWISTER_PATH" ]
then
	echo TWISTER_PATH set to: "$TWISTER_PATH".
	$PYTHON $TWISTER_PATH/server/resourceallocator/ResourceAllocator.py $RA_PORT
else
	echo TWISTER_PATH variable enviroment is not defined!
fi
